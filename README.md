# Battery-Checker
Displays a notification whenever the battery level falls below a certain percentage. Specifically used for Macbooks since
the battery level can't be seen if the window currently being used is in full screen mode.

Used the Cron scheduler to make sure the script runs every minute. 
